## Plan 6.1 Summary — Fix Modal + Reorder Navbar

### What was done
1. **PdfPreviewModal.tsx** — Replaced the raw GitHub URL in the `DialogDescription` with `"Review, preview, or download the document below."`. Replaced the raw URL in the footer `<small>` with `"PDF Document"` (hidden on mobile). Download and Open-in-new-tab buttons unchanged.
2. **Header.tsx** — Reordered nav links: `Home → Papers/PPTs → About → Bulk Download → Upload`. Previously About was after Bulk Download.

### Verification
- `npm run build` → exit code 0 ✓

### Status: ✅ COMPLETE
