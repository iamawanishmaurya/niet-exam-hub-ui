## Plan 6.2 Summary — Faculty Name Metadata

### What was done
1. **src/types/examPaper.ts** — Added `faculty_name?: string` to `AssetMappingItem`, `NormalizedPaper`, and `DisplayPaper` interfaces.
2. **src/hooks/useExamPapers.ts** — Added `faculty_name: paper.faculty_name` in `toDisplayPaper()` to pass the field through.
3. **src/components/PaperCard.tsx** — Added conditional badge: `{paper.faculty_name && <Badge>{paper.faculty_name}</Badge>}` rendered after the `branch` badge.

### Verification
- `npm run build` → exit code 0 ✓
- Badge will render when `faculty_name` data exists in `asset_mapping.json`; silently absent otherwise.

### Status: ✅ COMPLETE
